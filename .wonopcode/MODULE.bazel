# This file creates a workspace boundary to prevent crate_universe from scanning this directory.
# The .wonopcode directory contains snapshots that should not be included in the build.
module(name = "wonopcode_snapshots_boundary")
