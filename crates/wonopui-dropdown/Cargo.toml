[package]
name = "wonopui-dropdown"
version = "0.1.0"
edition = "2021"
authors = ["Troels F. Rønnow <troels@wonop.com>"]
description = "dropdown component for WonopUI"
license = "MIT"
repository = "https://github.com/wonop-io/wonopui"

[dependencies]
wonopui-core = { path = "../wonopui-core" }
wonopui-popover = { path = "../wonopui-popover" }
yew = { version = "0.21", features = ["csr"] }
