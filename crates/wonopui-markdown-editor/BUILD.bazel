load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//visibility:public"])

exports_files(["Cargo.toml"])

rust_library(
    name = "wonopui-markdown-editor",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "wonopui_markdown_editor",
    edition = "2021",
    deps = [
        "//crates/wonopui-core:wonopui-core",
        "@crates//:web-sys",
        "@crates//:yew",
        "@crates//:wasm-bindgen",
    ],
)

# Source files for Tailwind CSS scanning
filegroup(
    name = "srcs",
    srcs = glob(["src/**/*.rs"]),
)
