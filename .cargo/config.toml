# Enable getrandom's wasm_js backend for WASM targets
# This is required for getrandom 0.3.x compatibility with wasm32-unknown-unknown
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
